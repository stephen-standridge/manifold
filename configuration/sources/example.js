// import * as RX from 'rx/dist/rx.all';
// import * as RXDom from 'rx-dom/dist/rx.dom.js';

module.exports = {
	// attributes: {
	// 	sourceName: {		
	// 		// stream: RXDom.DOM.ajax({ url: '../../assets/morphTargets.json', responseType: 'json'})
	// 		// .singleInstance()
	// 		// .flatMap( (data) => {}),		
	// 		// identify: ( context, item )=>{ return context.name.split('_skull').join('') == item.get('name') }
	// 	}	
	// }
}